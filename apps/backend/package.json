{
    "name": "stonkses-backend",
    "module": "src/index.ts",
    "type": "module",
    "private": true,
    "scripts": {
        "dev": "bun run --watch api/dev.ts",
        "build": "tsc --noEmit",
        "lint": "biome check --write",
        "check-types": "tsc --noEmit",
        "db:push": "drizzle-kit push",
        "db:studio": "drizzle-kit studio",
        "deploy": "vercel --prod",
        "deploy:preview": "vercel",
        "deploy:production": "vercel --prod"
    },
    "devDependencies": {
        "@repo/biome-config": "^",
        "@repo/robinhood": "^",
        "@repo/typescript-config": "*",
        "@types/better-sqlite3": "^7.6.13",
        "@types/bun": "latest",
        "drizzle-kit": "^0.31.8",
        "vercel": "^39.2.9"
    },
    "peerDependencies": {
        "typescript": "^5"
    },
    "dependencies": {
        "better-auth": "^1.4.18",
        "drizzle-orm": "^0.45.1",
        "robinhood": "^1.8.0"
    }
}
